"""
tUilKit output subsystem — terminal windowing, compositing, and rendering.

Subpackages:
    backend     — RenderBackendInterface and AnsiRenderBackend
    zorder      — Z-order management for layered windows
    window      — Window dataclass and WindowManager
    draw        — DrawContext (clipping) and border drawing utilities
    widgets     — Widget base class for renderable UI components
    compositor  — Compositor that assembles the final terminal frame
"""
